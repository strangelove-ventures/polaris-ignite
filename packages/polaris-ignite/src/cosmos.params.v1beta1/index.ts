import Module from "./module";
import { queryClient, registry, txClient } from "./module";
import { msgTypes } from "./registry";

export * from "./types";
export { Module, msgTypes, queryClient, registry, txClient };
